export const metadata = { title: 'AfDB ROE Portfolio Intelligence Dashboard', description: 'Regional Operations Envelope Portfolio Analysis' };
export default function RootLayout({ children }) {
  return (<html lang="en"><body style={{margin:0}}>{children}</body></html>);
}
